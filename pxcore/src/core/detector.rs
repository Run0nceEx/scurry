use super::net::{Identifier, Connector};


// pub trait Protocol<C>: Connector + Identifier<C> {}

// async fn is_protocol<C: Connector, T: Identifier<C>>(scanner: T, connector: C) -> bool {
//     x.scan(connector).await.is_ok()
// }

// async fn detect<C: Connector, T: Identifier<C>>(scanner: T, connector: C) {
    
// }